program test
  use def_par
  implicit none
  Double precision,allocatable::H(:,:)
  alp1=1.0
  bet1=0.0
  del1=0.0
  N1=10
  allocate(H(0:N,0:N))
  call U2_ham(alp1,bet1,del1,0.0d0,N,H)
  write(*,*) H
end program test
